syntax = "proto3";

// 服务定义
service Receiver {
  rpc receive (Data) returns (Reply) {}
}

// Data define
message Data {
  string node_name = 1;
  string key = 2;
  bytes data = 3;
}

// 返回消息定义
message Reply {
  bool succeed = 1;
}


