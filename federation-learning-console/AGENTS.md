# federation-learning-console — AGENTS.md

This repo is the generated Refine / React frontend for the Federation Learning platform.

## Source Of Truth

- The domain model is `/Users/bryce/codes/medo/event-modeling/medol/examples/fl/federation-learning.medol`.
- Frontend pages, resources, schemas, command forms, and API wiring are generated by `/Users/bryce/codes/medo/event-modeling/es-code-generator`.
- If a generated UI behavior should apply generally, fix the Refine generator templates instead of hand-editing generated pages.
- If a page needs business-specific customization, add or use a stable hand-written extension point rather than editing generated files directly.

## Frontend Rules

- Keep identifiers, routes, resource names, command names, and API paths in English.
- Use the generated API capability metadata rather than hard-coded resource or command names.
- File fields follow MEDOL semantics:
  - `uploadFile` means upload binary content to the support file-upload endpoint and submit the returned reference.
  - `file` means submit an existing file reference string.
- Do not commit `.env`, `node_modules`, `dist`, or local generated artifacts.

## Validation

```bash
pnpm run build
```

Use the browser only when visual behavior or form submission must be verified interactively.
